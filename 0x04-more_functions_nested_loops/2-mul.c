#include "main.h"
/**
 * mul -  function that multiplies two integers.
 * @a: first value to be checked
 * @b: Second value to be checked
 * Return: 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
